#Introduction</p>

It is of paramount importance for one to consider the very important role that Rust can play.</p>
There are many components that are being re-written in Rust, partially if not fully.</p>
We can look into the famous changes in Python components such as the package manager.</p>

Below is a good article on how to design a component in Rust:</p>
https://tiberiusferreira.github.io/blog/posts/designing_autograd_system_rust_first_steps/
https://rufflewind.com/2016-12-30/reverse-mode-automatic-differentiation